# Same-camera lateral-recovery pair

This pair is the requested visual clarification for lateral recovery. Both
images use the same fixed camera, crop, lens, lighting, snapshot, and robot
configuration. Only the scripted lateral offset changes:

- `02_contact_misalignment.png`: larger lateral peg--hole error;
- `03_lateral_recovery.png`: reduced lateral peg--hole error, with the wrist,
  end effector, peg, fixture, and hole still visible.

Place the two images side by side in the thesis and describe the change as a
scripted conceptual illustration. It is not a Formal64 trajectory, policy
output, or quantitative result. Any directional arrow should be added in the
figure layout, not baked into these raw PNGs.
