# Progress report — 2026-07-06

## Current result

The controlled data-collection pipeline completed successfully and produced the dataset used for the visual-only SmolVLA baseline. The baseline training run was still active when this report was recorded.

## Collection and dataset audit

- Raw collection: `franka_pickplace_joint_torque_w30_20260706T130538Z`
- Episodes: 200
- Total frames: 41,276
- Raw HDF5 size: approximately 7.3 GB
- Torque range: `[-57.5694, 19.1751]`
- State schema: 9 values (`7` arm joint positions + `2` gripper positions)
- Action schema: 8 values (`7` absolute arm targets + `1` gripper command)
- HDF5 SHA-256: `09a83546afa456efc1e0593dc431da8231b39eb455b9b725b477dacb30bad60f`

The finalized HDF5 passed the structural audit. Isaac Sim exited with status 134 during teardown after collection because a tiled-camera cleanup callback referenced an object that had already been released. The HDF5 had already been finalized; the checksum and full audit passed, so the teardown failure did not invalidate the collected data.

The converted LeRobot dataset is `franka_pickplace_joint_visual_torque_w30_v1`. Validation confirmed:

- shared policy interface: `state[9]`, two cameras `[3,224,224]`, and `action[8]`;
- torque history: `observation.gripper_torque` with shape `[30,1]` and the newest value at index `-1`;
- 1,024 sequential torque windows audited without an episode-boundary error;
- six focused interface and torque-LSTM tests passed.

Dataset binaries and model weights remain outside normal Git history.

## Baseline training

The pure visual SmolVLA baseline started from the official local `lerobot/smolvla_base` weights with:

- seed: `1000`;
- batch size: `8`;
- target: `50,000` optimizer steps;
- checkpoint interval: `5,000` steps;
- torque-LSTM disabled, so torque is excluded from the baseline policy.

At `2026-07-06 16:42 BST`, training had reached approximately step 21,000 of 50,000. The latest reported loss was `0.015`, with gradient norm `0.296`. Checkpoints at 5k, 10k, 15k, and 20k were present. No training exception was present in the log at that point.

## Workflow changes captured in this update

1. `inspect_raw_hdf5.py` now enforces the actual 9-dimensional joint state.
2. `rebuild_dataset.sh` distinguishes a simulator teardown failure after a finalized HDF5 from a collection failure that produced no data; every retained HDF5 is still audited.
3. `preflight_collection.sh` bounds the Isaac headless GPU smoke test and verifies Vulkan/NVIDIA initialization before accepting a timeout.
4. `post_collection_baseline_50000.sh` checks the HDF5 checksum, audits and converts the dataset, validates both policy interfaces, runs focused tests, and starts the non-overwriting 50k baseline job.

## Remaining work

1. Let the visual-only baseline reach 50,000 steps and record its final checkpoint and metrics.
2. Train the matched torque-LSTM model with the same dataset, seed, batch size, and step budget.
3. Evaluate both checkpoints with the same headless task configuration and report the controlled comparison.
